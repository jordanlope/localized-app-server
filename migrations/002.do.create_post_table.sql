CREATE TABLE post (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    businessId INTEGER REFERENCES realtors(id) ON DELETE SET NULL,
    postTitle TEXT NOT NULL,
    description TEXT NOT NULL,
)